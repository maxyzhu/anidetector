# 项目简报：野生动物视频活动预算流水线

> 这份文档是给 Claude Code 的项目上下文。请在动手前完整读一遍，特别是"明确不做的事"和"已知陷阱"两节 —— 那里面的每一条都是踩过或查证过的，不是猜测。

---

## 1. 一句话定位

**输入连续视频，输出「按物种分组的轨迹 + 每条轨迹的活动/静止时间段」，不定义任何行为语义。**

关键词：`ethogram-free`（不需要预定义行为谱，因此不需要行为标注数据）。

---

## 2. 为什么这个缝隙存在

已被占据的位置（不要去碰）：

| 能力 | 谁做了 | 状态 |
|---|---|---|
| 视频 → 物种标签 | Zamba / Zamba Cloud | 免费开源，已解决 |
| 廉价筛帧 → 昂贵分类的级联 | Zamba（`megadetector_lite_yolox`） | 已实现 |
| RGB 植被场景背景减除 | Zamba 评估后否决 | 公认的坑 |
| 平台化 + Camtrap DP + GBIF | TRAPPER | 已获资助 |
| 围栏内逐个体轨迹 | PantherAI / 北极熊框架 / LabGym | 已发表 |

空缺：

- Zamba 输出「这段视频里有什么物种」，**不输出轨迹数、停留时长**。
- 动物园研究做逐个体轨迹，但全部是**会话式采样**（典型 52–90 分钟/天，数个非连续日）。
- 连续录制的现实是 **25fps × 24h × 数月**。差了约四个数量级。

**结论：方法层面别人做完了，规模层面没人碰。本项目的差异化在规模，不在算法新颖性。**

---

## 3. 明确不做的事（防止 scope creep）

- ❌ **行为语义分类**。不判断"觅食/警戒/理毛"。这是研究问题，且领域内对 ethogram 无共识。
- ❌ **个体重识别（re-ID）**。v1 只做 track，不做 individual。文档中必须明示：**track ≠ individual，动物离开再返回会产生多条 track**。
- ❌ **姿态估计 / 关键点**。v1 不需要，时间预算不依赖它。
- ❌ **模型训练或微调**。全部使用现成权重。
- ❌ **自写 CPU 算子 / 量化内核**。ONNX Runtime 一个开关的事，ROI 极低。
- ❌ **云服务 / 上传用户数据**。社区调研显示不用云的首要原因是"本地够用"，其次是网络与机构政策。做本地/可自托管。
- ❌ **背景减除作为 RGB 场景的主要门控**。植被运动噪声太大，需逐相机调参。**仅在热成像上启用**。

---

## 4. 核心架构原则

### 4.1 存信号，不存判定

`bout`（活动回合）**不是存储的，是查询时计算的**。

阈值、最短 bout 时长、最大间隙合并 —— 全部是**查询参数**。

理由：阈值一定会有争议（不同物种、不同研究问题）。如果把判定烤进输出，每次改阈值都要重跑几 TB 视频。存连续信号的话，重新阈值化是毫秒级。

物种标签**是存储的**，因为重算要跑模型。这两者的区别是整个 schema 的核心。

### 4.2 分类在 track 级，不在 frame 级

复杂度从 `O(帧数)` 降到 `O(轨迹数)`。

一夜 200 万帧、200 条轨迹、每条挑 10 帧 → **2000 次分类调用，不是 200 万次**。

流程：先跟踪 → 轨迹结束 → 从轨迹里按质量挑 N 帧 → 分类 → 投票聚合成 track 级标签。

### 4.3 跟踪与采样解耦

跟踪需要**时间密度**（5–10 fps），分类需要的不是速率而是**每轨迹 N 帧**。这两个是独立的旋钮，不要用同一个 fps 参数控制。

---

## 5. 数据模型

```
deployment          相机 × 地点 × 时间段（顶层单位，不是"视频文件"）
  ├─ location, lat, lon, start_ts, end_ts, camera_id, modality(rgb|thermal)
  │
  └─ media          单个视频文件（存储切分产物，动物可跨文件）
       ├─ path, start_offset_ts, fps, gop_size, duration
       │
       └─ track     一段连续观察
            ├─ track_id, media_id, start_frame, end_frame, start_ts, end_ts
            ├─ species_label        投票结果
            ├─ species_votes        完整投票分布（JSON）
            ├─ species_agreement    一致率 0–1
            ├─ taxon_path           纲;目;科;属;种（来自 SpeciesNet）
            │
            └─ motion_signal[]      时间序列，每采样点一行
                 ├─ ts
                 ├─ displacement_bl_per_s    位移，单位：体长/秒
                 ├─ deformation_score        bbox 内像素变化量
                 └─ bbox                     x, y, w, h
```

**`bout` 无表**。由 `motion_signal` + 查询参数实时计算。

### 为什么顶层是 deployment 而不是 video

对连续录制，视频文件只是存储切分。更重要的是：**Camtrap DP 标准的结构就是 deployments / media / observations**。一开始对齐，将来导出和发布 GBIF 是零成本；不对齐则需要重构数据模型。

### 物种标签是一棵树，不是扁平字符串

SpeciesNet 在不确定时会做 **label rollup**，把预测上推到分类树里第一个得分足够高的祖先，所以输出里会混有「狍子 / 鹿科 / 哺乳纲 / 动物」不同层级的标签。

**因此 filter 必须支持按分类层级聚合**，不能只做字符串相等。用户点"鹿"应同时看到狍子和马鹿。分类树在 SpeciesNet 输出中已有，不用自建。

建议开启**地理围栏**（传入 ISO 3166-1 alpha-3 国家码），能显著减少荒谬结果。

---

## 6. 技术栈

### 第 1 层 解码

**唯一解码依赖：PyAV。** 不要用 `cv2.VideoCapture`（拿不到包级信息），**不要引入 NVIDIA DALI**（理由见下）。

#### M0 就要开的两个开关

```python
import av

with av.open(path) as container:
    stream = container.streams.video[0]

    # 1. 只解关键帧。解码器层面跳过，不是解了再丢。
    stream.codec_context.skip_frame = "NONKEY"

    # 2. FRAME 级多线程。PyAV 在解码时释放 GIL，这是真并行。
    stream.thread_type = "AUTO"

    for frame in container.decode(stream):
        ts = float(frame.pts * stream.time_base)   # 注意：不是 frame.index
        ...
```

- **只解关键帧是最大的免费收益。** h264 的 P/B 帧必须依赖前后帧重建，跳过它们意味着**完全不做那部分解码运算**。GOP=50 时，25fps 视频的关键帧只有 0.5fps。
- **坑**：用了 `skip_frame` 之后 `frame.index` 失去意义，**必须用 `frame.pts`** 换算时间戳。时长计算依赖这个，错了整个输出就废了。
- **坑**：开 `thread_type="AUTO"` 后，喂进 packet 到拿到结果之间的延迟会明显变大，帧的产出顺序不再与喂入顺序严格对应。**这正是 M2 需要 reorder buffer 的原因** —— 官方文档自己点出了这个行为变化。
- PyAV 在实际解码时**释放 GIL**，所以 Python 多线程解码是真并行。先用 `ThreadPoolExecutor` 喂一个推理线程，可能直接让多进程和跨进程拷贝变得没必要。

#### 硬件解码（M2，可选路径，不做硬依赖）

两条路，都必须能优雅回退到软解：

1. PyAV 的 hwaccel 配 NVDEC / QuickSync / VAAPI —— 保持单一依赖
2. ffmpeg CLI 子进程 `-hwaccel cuda|qsv` 通过管道读原始帧 —— 丑但极稳，ffmpeg 二进制到处都有

#### 为什么不用 NVIDIA DALI

三条硬约束，任何一条单独都足以否决：

1. **前置条件是 Linux x64 + 支持 CUDA 的 NVIDIA 驱动。** 没有 Windows、没有 macOS、没有 GPU 装不上。目标用户是 Windows 笔记本上的生态学家，社区调研中"本地够用"是不用云的首要原因，安装无痛是采用率的前提。
2. **不支持可变帧率。** 文档明写"只支持恒定帧率视频"。标准 VideoReader 遇到 VFR 会断言失败，早期版本甚至**无限挂死只能 kill 进程**；而且 VFR 检测本身会误判（同一个 h264 流用不同 `-r` 封装出的 mp4 会被拒）。野外设备录像恰恰是帧率不规整、时间戳异常的高发区。这个 issue 从 2019 年问到现在。
3. **它是训练用的数据加载器，不是视频处理库。** 输出形状是 `(N, F, H, W, C)`，为"随机采样定长片段 + 增强喂给 DNN"设计。我们要的是"按时间顺序走完长视频 + 精确时间戳"，**访问模式相反**。社区里有人想做完全一样的事（25fps 视频按 2fps 抽帧、长度不一），困在"不知道怎么合理摆脱 sequence_length"。

唯一可保留的位置：如果将来自己有一台 Linux + GPU 批处理机，可以把 DALI 作为**那台机器上的**加速路径。但那是运维选择，不是产品依赖，而且维护两条解码路径的成本大概率不值得。

### 第 2 层 采样 / 门控

按成本递增：

| 手段 | 状态 |
|---|---|
| 固定步长 | ✅ M0 |
| 仅关键帧（`skip_frame="NONKEY"`） | ✅ M0，最大免费收益 |
| 像素域背景减除 | ⚠️ **仅热成像**。RGB 植被场景是公认的坑（见 7.5） |
| 自适应升档（命中后临时提高帧率） | M2 |
| ~~压缩域运动矢量门控~~ | ❌ **已否决，见下** |

#### 更正：压缩域运动门控不成立

**早期版本的本文档声称"运动矢量可在压缩域做门控，比解码后做背景减除便宜一个数量级"。这是错的，已删除。**

实际情况：

- 运动矢量通过 `codec_context.export_mvs = True` + `frame.side_data.get("MOTION_VECTORS")` 获取，但**必须先解码该帧才能拿到**，省不掉解码成本。PyAV 社区里正有人问"有办法不完整解码就拿到运动矢量吗"，答案是没有。
- 部分容器/流上 side data 恒为空（有人在 h264/rtsp 上验证过矢量确实存在，但 PyAV 取不到）。
- 真正的压缩域提取需要专门工具（`mv-extractor` 那一类），不是 PyAV 顺手能做的事。

**结论：不写进任何里程碑。** 如果将来真想做，先花半天在真实素材上验证能不能拿到矢量，再谈。

### 第 3 层 检测

- **MegaDetector v6**。
- **许可关键**：必须用 `MDV6-apa-rtdetr-e`（Apache-2.0，且精度最高）或 `MDV6-mit-yolov9-e`（MIT）。
- 加载必须用 `MegaDetectorV6Apache` / `MegaDetectorV6MIT` 类，**不是通用的 `MegaDetectorV6()`**。默认变体走 ultralytics，是 AGPL-3.0，会传染整个项目。
- 推理后端：**ONNX Runtime**（跨平台）。微软已发布打包好的 ONNX 模型库（含 `manifest.toml`），可直接消费，省掉导出工作。

### 第 4 层 跟踪 —— 核心新增算法

范式：**tracking-by-detection**。

组成：
1. **运动模型** —— 卡尔曼滤波，状态 `[cx, cy, aspect, h, dcx, dcy, ...]`。相机固定 + 恒速模型足够。
2. **关联代价** —— IoU / 中心距 / 外观 embedding 构成代价矩阵，用**匈牙利算法**（`scipy.optimize.linear_sum_assignment`）求最优匹配。
3. **生命周期状态机** —— `tentative`（连续命中 N 次才确认）→ `confirmed` → `lost` → `deleted`。

**实现顺序：先手写 SORT（约 200 行），再换 ByteTrack 做对比。**

手写的理由：你需要理解卡尔曼和匈牙利在这个问题上的实际行为，否则后面调 `max_age` 就是瞎猜。而且两者的对比数据本身有价值。

ByteTrack 的关键洞察是**低置信度检测框也用于二次关联**而非直接丢弃，正好对应"动物被植被部分遮挡"。相机固定，所以不需要 BoT-SORT 的相机运动补偿。

现成实现：`supervision` 库的 `sv.ByteTrack`（MIT，项目已在用 supervision，零新依赖）。`boxmot` 要查许可，含 AGPL 组件。

### 第 5 层 track 级分类

- **SpeciesNet**（Apache-2.0，代码与权重均可商用）。
- **注意**：SpeciesNet 默认 ensemble 会自动下载 MegaDetector **v5a** 作为检测器，那是 AGPL。必须手动替换成上面选定的 v6 变体。
- 帧挑选按质量排序，不要随机：bbox 面积最大 / 检测置信度最高 / 清晰度最高（拉普拉斯方差，滤运动模糊）/ 姿态多样性（避免连续 10 帧）。
- 投票聚合，**同时存投票分布和一致率**。一致率低 = 值得人工复核 → 免费得到一个「按可疑度排序的复核队列」。

### 第 6 层 运动信号

两个必须都算的独立信号：

| 信号 | 计算 | 捕捉 |
|---|---|---|
| **位移** | bbox 中心跨帧移动 | 走动、奔跑、离开 |
| **形变** | bbox 区域帧差 | 进食、理毛、张望 |

一头鹿站着咀嚼：位移=0，形变≠0。只输出位移会丢掉行为学上最想要的区分维度。

**位移必须用体长归一化**：`displacement / bbox对角线长度`，单位「体长/秒」。这样远近距离的同一物种得到可比数值，**消掉了"每台相机都要调阈值"的大部分问题**，且体长/秒在行为学上是真实使用的速度单位。

### 第 7 层 聚合与导出

- track → 时长表
- **独立事件分组**：生态学惯例，间隔小于 N 分钟算同一事件（相机陷阱常用 30 分钟）
- **可回溯索引**：帧号 → 时间戳 → 原视频可 seek 偏移。**这个功能不能省** —— 生态学家必须能验证每一个判断，否则不会在论文里引用你的数字。
- **Camtrap DP 格式导出**

---

## 7. 已知陷阱

### 7.1 低帧率下 IoU 关联会失效 ⚠️ 最重要

IoU 关联的前提是"同一目标在相邻帧的框有重叠"。降到 1fps 后，一头走动的鹿两帧之间移动了好几个身长，**IoU = 0，关联直接断裂**，一条轨迹被切成几十段。

解法（按优先级）：
1. **解耦速率** —— 跟踪跑 5–10fps，分类按轨迹挑帧。**这是正解。**
2. 换关联度量 —— 外观 embedding 代替 IoU
3. 放大关联门 + 加强运动预测 —— 最脆弱

**这是设计约束，不是实现细节。架构里必须体现。**

### 7.2 `max_age` 是全局最敏感的参数

轨迹丢失后保留多少帧。设小了轨迹碎片化、个体数虚高；设大了不同个体被合并。**与采样帧率强耦合。**

它的敏感性曲线应该是测试集产出的第一张图。

### 7.3 bout 分割需要迟滞，否则会得到几百段

原始逐帧动/停判定会剧烈抖动，动物停顿一秒就切一次。需要三个参数（**全部是查询参数**）：

- **进入阈值 > 退出阈值**（迟滞，防止在阈值边缘反复横跳）
- **最短 bout 时长**，短于 N 秒的段并入相邻
- **最大间隙合并**，两段活跃间的短暂静止小于 M 秒则合并

领域内"多长间隔算独立事件"有专门概念叫 **bout criterion interval**，有从数据估计它的标准方法。用领域已有词汇，用户和审稿人接受度更高。

### 7.4 并行性边界

**跟踪在时间上串行依赖** —— 第 N 帧的关联依赖第 N-1 帧的轨迹状态。**单视频内跟踪无法并行。**

但**跨视频/跨夜完全并行**。这是扩展轴。

架构：视频级并行 × 视频内流水线并行（解码 / 推理 / 跟踪三级）。**不要试图并行跟踪本身。**

#### 先用多线程，不要一上来上多进程

PyAV 在实际解码时**释放 GIL**，所以多线程解码是真并行。而 Python 多进程之间传帧要走 pickle → 管道 → unpickle，**一张 4K 帧就是一次完整拷贝**，这是整条流水线里唯一真正值得用 C++ 解决的痛点。

顺序：
1. `ThreadPoolExecutor` 多线程解码 + 单推理线程 —— 先试这个
2. profile 看核利用率。**如果已能打满核，到此为止，不要写 C++**
3. 只有在确认必须多进程、且拷贝占比高的情况下，才上共享内存 + 无锁环形缓冲（C++ / pybind11）

第 3 步的理由是**内存所有权和同步原语**，不是"C++ 比 Python 快"。解码本身已经是 C（libavcodec 手写 SIMD），推理已经是 C++（ONNX Runtime）—— 用 C++ 重写这两层最好的结果是打平。

C++ 部分必须是**可选加速路径**，不是必需依赖：

```python
try:
    from _fastbuf import RingBuffer      # C++ 扩展
except ImportError:
    from .pybuf import RingBuffer        # 纯 Python 回退
```

否则要为 Windows / macOS(x86+ARM) / Linux 各构建 wheel，用户装不上就得本地编译，而生态学家的电脑上没有编译器。**这对采用率是致命的。**

### 7.5 热成像是不同模态

MegaDetector 和 SpeciesNet 都在 RGB 上训练。**直接跑热成像大概率不行，必须先验证。**

但有个反向优势：MegaDetector 维护者明确说过「背景减除在热成像上效果很好」—— Zamba 在 RGB 植被场景放弃的那条路，在热成像上是通的。

---

## 8. 里程碑

### M0 — 端到端跑通（不优化）

```
PyAV 顺序解码
  skip_frame="NONKEY"      ← 免费大收益，M0 就开
  thread_type="AUTO"       ← 免费并行，M0 就开
  时间戳用 frame.pts        ← 不是 frame.index
  → 固定步长抽样
  → MegaDetector (ONNX Runtime)
  → 手写 SORT
  → track → 时长表 (JSON)
```

上面两个开关是 M0 的例外：它们虽然是优化，但成本是两行代码，且会改变后续所有测量的基线。开着测。

同步、单线程、丑陋都无所谓。**这是后续所有加速比的基线，没有它就没有可讲的数字。**

唯一必须守住的设计约束：**解码与推理之间用队列而非直接函数调用**，即使 v1 队列深度为 1、单线程。这样 M2 换成真正的并发流水线时改的是实现不是结构。

### M1 — 正确性

- 接入 SpeciesNet track 级分类 + 投票
- 运动信号 + bout 查询
- **宠物测试集验证**（见下节）
- 换 ByteTrack 并与 SORT 对比

### M2 — 吞吐（简历价值在这里）

- 解码 / 推理解耦 + 环形缓冲
- 硬件解码（NVDEC / QSV）
- 动态批处理（每帧裁切数 0..N 不等，需跨帧拉平成固定 batch）
- **reorder buffer**（推理乱序完成，但跟踪必须按时间有序 —— 这是最有含量的并发问题）
- 按 media / 按夜 checkpoint（季度级任务一定会崩，要能续跑）

---

## 9. 验证策略

### 为什么必须先建测试集

野外数据**没有真值**。如果跑出「3 号轨迹停留 15 分钟」，没人能说对不对，整个交付物的可信度是零。

参考：一份蜘蛛猴研究为建立人工基线，动用了**六名观察者 + 一致性检验（最低一致率 85%）**。你没有六个观察者。

### 用宠物录像做真值

自己家宠物录几小时（多房间更好，能造出真正的进出场景），手工标注：几只、何时进、何时出、何时动/停。

它精准命中最难验证的部分：

| 失效模式 | 野外可验证 | 宠物可验证 |
|---|---|---|
| 两只被合并成一只 | ❌ | ✅ |
| 一只被拆成两条轨迹 | ❌ | ✅ |
| 离开又返回被算成两个 | ❌ | ✅ |
| 时长偏差 | ❌ | ✅ |

附带价值：这是唯一可以公开发布的验证数据（野生动物数据有敏感位置问题）。

### 第一张必须产出的图

**bout 数量与总活跃时长，对「阈值」和「最短 bout 时长」的敏感性曲线。**

如果活跃时长在阈值 0.2–0.5 之间变化 3 倍，工具输出不可用，必须先解决。如果相对稳定，这张图本身就是可信度证明。

---

## 10. 必读的先验工作

动手前读，否则会重复造轮子：

| 工作 | 为什么读 |
|---|---|
| **Sherlock**（Penn et al., Methods in Ecology and Evolution, 2024） | 标题含 "low-resource"，最接近的资源效率导向工作 |
| **PantherAI**（zoo-housed tiger, activity budget & space use） | 输出形态与本项目最接近，抄它的评估指标 |
| **北极熊框架**（Zuerl et al., Nuremberg Zoo, Animals 2022） | 逐个体轨迹的已发表方法 |
| `zamba/object_detection/yolox/megadetector_lite_yolox.py` | 他们的筛帧策略。要么复用，要么说清为什么不用 |
| Context R-CNN / DeepMeerkat / FoxMask / EventFinder | 时序上下文与背景减除的各种路线 |

---

## 11. 术语（对外沟通时统一使用）

- 一段连续活动 → **bout**
- 整体输出 → **activity budget**
- 刻意不做的那件事 → **ethogram**
- 一句话卖点 → **ethogram-free activity budgets from continuous video**
